#include<iostream>
using namespace std;

int main(){

/*
    cout << "Namste Duniya" << endl;
    cout << "\n";
    cout << "Hii" << "\n";
    cout << "Hello \n";

    // without return 0 --> this is working
    
    int a = 4;
    cout << a << endl;

    char b = 'V'; // so here we can't give characters 'vk' like this we need to give single char.
    cout << b << endl;

    bool bl = true;
    cout << bl << endl;

    double d = 1.23;
    cout << d << endl;

    cout << sizeof(a) << endl << sizeof(b) << endl << sizeof(bl) << endl << sizeof(d) << endl;

*/

    // Type Casting 

/*
    int a = 'a';
    cout << a << endl;

    char ch = 98;
    cout << ch << endl;

    char ch1 = 123456 ; // it will give error due to extra value than requirement
    // to execute it , it takes some values from the last and according to ASCII value result will occured
    cout << ch1 << endl;

// How -ve number are stored
  // First bit --> 
       // +ve -> 0
       // -ve -> 1

*/

/*

     unsigned int a = 112; // it means stored only +ve numbers
     // if here we give -ve values then it will print very huge value due to unsigned 
     // means treated as +ve numbers 
     // becasue we know that for -ve numbers changing in to binary we need to take 2's complement
     // that's why MSB will 1 and treated as +ve numbers that's why it will give very huge value
     cout << a << endl;
*/


     // Operators

     int a = 2/5;
     cout << a << endl;


   // return 0;

}
